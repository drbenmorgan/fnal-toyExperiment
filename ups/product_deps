# The parent line must be the first non-comment line in the file
# This line defines the product name and version
parent	toyExperiment	v0_00_14
defaultqual	e4

# These optional lines define the installed directories where headers,
# libraries, and executables will be found.
#
# Format: directory_type directory_path directory_name
#
# The only recognized values of the first field are incdir, libdir, and bindir
# The only recognized values of the second field are product_dir and fq_dir
# The third field is not constrained.
#
# If a particular directory_type is not specified that item will not be
# set up in the UPS table file.
incdir      product_dir include
libdir      fq_dir      lib
bindir      fq_dir      bin

#
product		version
gcc		v4_8_1
art		v1_08_09

# list products required ONLY for the build
# any products here must NOT have qualifiers
only_for_build	cetbuildtools	v3_05_01

# e4  - with gcc 4.8.1 and -std=c++0x
# -nq- here means there is no qualifier
# a - here means the dependent product is not required by the parent and will not be setup
qualifier	art		gcc	notes
e4:debug	e4:debug	-nq-	-std=c++0x
e4:prof		e4:prof		-nq-	-std=c++0x
mu2e:e4:debug	mu2e:e4:debug	-nq-	-std=c++0x
mu2e:e4:prof	mu2e:e4:prof	-nq-	-std=c++0x
nu:e4:debug	nu:e4:debug	-nq-	-std=c++0x
nu:e4:prof	nu:e4:prof	-nq-	-std=c++0x

# table_fragment_begin
# this is a table file fragment
# it will be copied verbatim
# table_fragment_end

table_fragment_begin
pathPrepend(FHICL_FILE_PATH, ".:${UPS_PROD_DIR}")
pathPrepend(PATH, ".:"${UPS_PROD_DIR}/scripts)
table_fragment_end

# Preserve tabs and formatting in emacs and vi / vim:

### Local Variables:
### tab-width: 8
### End:

# vi:set ts=8 noexpandtab:
